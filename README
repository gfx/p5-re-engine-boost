This is Perl module re::engine::boost.

INSTALLATION

Install cpanm (App::cpanminus) and then run the following command to install
re::engine::boost:

    $ cpanm re::engine::boost

If you get an archive of this distribution, unpack it and build it
as per the usual:

    $ tar xzf re-engine-boost-$version.tar.gz
    $ cd re-engine-boost-$version
    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

re::engine::boost documentation is available as in POD. So you can do:

    $ perldoc re::engine::boost

to read the documentation online with your favorite pager.

LICENSE AND COPYRIGHT

Copyright (c) 2011, Fuji, Goro (gfx). All rights reserved.

This library is free software; you can redistribute it and/or modify
it under the same terms as Perl itself.
